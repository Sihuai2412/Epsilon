function fib(x : int) : int {
    if (x <= 0) {
        return 0;
    } else if (x == 1 || x == 2) {
        return 1;
    } else {
        return fib(x - 2) + fib(x - 1);
    }
}

function main() {
    print(fib(30));
}